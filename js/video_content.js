const selectLanguage = document.querySelector('.select-language');
const languageOptions = document.querySelectorAll('.select-language-option');
const languageConts = document.querySelectorAll('.language-cont');

const timeSlider = document.querySelector('.time-slider');
const mainVideoElement = document.querySelector('.main-video');



/* selectLanguage.addEventListener('click', ()=> {
    console.log(selectLanguage.value);

    languageConts.forEach(e => {
        e.classList.add('display-none');

        if(e.classList.contains(selectLanguage.value)) {
            e.classList.remove('display-none');
        }
    });
});
 */
